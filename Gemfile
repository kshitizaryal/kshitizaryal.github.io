source 'https://rubygems.org'

gem 'github-pages', :group => [:jekyll_plugins, :development, :test]

group :development do
  gem 'wdm' if Gem.win_platform?
  gem 'scss_lint', :require => false
end

group :test do
  gem 'html-proofer'
end
